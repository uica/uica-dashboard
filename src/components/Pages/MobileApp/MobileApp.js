import React from "react";
import HomeSlide from "../../Mobile/HomeSlide";
import PageContainer from "../../utils/PageContainer";

const MobileApp = (props) => {
  return (
    <PageContainer title="Mobile App">
      <HomeSlide />
    </PageContainer>
  );
};

export default MobileApp;
